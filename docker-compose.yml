version: '3.8'

services:
  bioreason-x:
    build: .
    container_name: bioreason_x_platform
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
